/// Spacing and shape tokens from `App design/minimalist_slate/DESIGN.md`.
abstract final class AppDimens {
  /// Bento cell corner radius (radius-lg).
  static const double cellRadius = 24;

  /// Manga cover corner radius inside cells.
  static const double coverRadius = 12;

  /// Pill buttons (radius-xl / stadium).
  static const double pillRadius = 32;

  /// Gap between bento cells.
  static const double gutter = 16;

  /// Internal padding of bento cells / screen edges.
  static const double cellPadding = 24;

  /// Base spacing unit.
  static const double unit = 8;

  /// Manga cover aspect ratio (width / height).
  static const double coverAspectRatio = 2 / 3;
}
